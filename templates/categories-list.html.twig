<h4 class="stext-301 cl0 p-b-30">
    Categories
</h4>

<ul> 
    {% for category in categories %}
    <li class="p-b-10">
        <a href="{{ path('products', {current_category: category.name}) }}" class="stext-107 cl7 hov-cl1 trans-04">
            {{ category.name }}
        </a>
    </li>
    {% endfor %}
</ul>